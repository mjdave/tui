{
    passCount = 0
    failCount = 0

    assert = function(pass, info)
    {
        if(pass)
        {
            passCount++
        }
        else
        {
            failCount++
            print("test fail! ", info)
        }
    }
}