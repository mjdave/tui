#testCommon = require("testCommon.tui")
#assert = testCommon.assert
#getLineNumber = debug.getLineNumber

#tempTestFile = require("tempTestFile.tui")

#nilFunc() #Attempt to call missing function 'nilFunc'
#foo.nothing() #Attempt to call missing function 'nothing'. 'foo' is valid, but 'foo.nothing' is nil
#foo.bar() #Expected function 'bar', found string. 'foo.bar' is not a function
#bar.foo() #Attempt to use '.' to access from nil object 'bar'

testFunc = function()
{
    #nilFunc() #Attempt to call missing function 'nilFunc'
    #foo.nothing() #Attempt to call missing function 'nothing'. 'foo' is valid, but 'foo.nothing' is nil
    #foo.bar() #Expected function 'bar', found string. 'foo.bar' is not a function
    #bar.foo() #Attempt to use '.' to access from nil object 'bar'
}

testFunc()