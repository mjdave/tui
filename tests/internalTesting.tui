testCommon = require("testCommon.tui")
assert = testCommon.assert
getLineNumber = debug.getLineNumber

tempTestFile = require("tempTestFile.tui")

print("hello this is top level")
debug.backtrace()

testFunc = function()
{
    innerFunc = function(stuff, otherFunc)
    {
        print("hello this is innerFunc")
        debug.backtrace()
    }

    print("hello this is testFunc")
    debug.backtrace()

    innerFunc()

    tempTestFile.testFunc()
    print("testFunc done")
}
testFunc()

print("done")