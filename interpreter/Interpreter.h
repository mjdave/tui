#ifndef __Interpreter__
#define __Interpreter__

#include <stdio.h>

#include "MJScript.h"


class Interpreter {
    
public:
    Interpreter(std::vector<std::string>& args);
    ~Interpreter() {};
    
private:
};

#endif
